# ops-map
A Chrome extension that replaces the new tab page with a visual map of your active campaigns, projects, and missions. Context resumption tool for people running multiple parallel tracks of work.
